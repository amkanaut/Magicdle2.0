import React from 'react';

const FeedbackRow = () => {
  return (
    <div className="feedback-row">
      {/* Displays result of a guess (Older/Newer release date) */}
    </div>
  );
};

export default FeedbackRow;

/**
 * FeedbackRow Component
 * Responsibility: Shows historical feedback for previous guesses.
 */
